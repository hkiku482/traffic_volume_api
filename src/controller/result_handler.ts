export const resultHandler = async (locationsId = ''): Promise<string> => {
  if (locationsId === '') {
    // get all result
  } else {
    // get any result
  }
  return JSON.stringify({
    results: [],
  });
};
